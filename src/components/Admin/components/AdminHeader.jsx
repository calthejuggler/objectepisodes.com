import React from 'react'
import {withAuth} from '../../Session/withAuth'

const AdminHeader = (props) => {
    const {user} = props
    return (
        <div>
            
        </div>
    )
}

export default withAuth(AdminHeader)
